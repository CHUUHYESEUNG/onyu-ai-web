# CODEX 작업 기록

## 2025-11-04

- `app/page.tsx`를 다시 구성해 Landing → Editor → Result → Publish 단계가 이어지도록 상태 전환 로직 복원 및 신규 콜백 추가.
- `components/result-page.tsx`에 “편집 완료” CTA를 추가해 출판 플로우로 진입 가능하도록 연결.
- `components/publishing-flow.tsx`를 도입해 3단계 마법사(UI): 옵션 선택 → 세부 설정 → 견적/다음 단계 요약을 제공하고, 선택 옵션에 따라 예상 견적을 동적으로 계산.
- 편집 화면(`/projects/[projectId]/edit/page.tsx`) 상단에 “임시 저장”·“편집 완료” 액션을 우측에 배치해 작업 흐름이 출판 단계로 자연스럽게 이어지도록 UX 정비.
- 출판 준비 라우트(`/projects/[projectId]/publish/page.tsx`)를 추가하고 `PublishingFlow`를 연결해, 편집 완료 후 바로 옵션 선택 마법사를 확인할 수 있도록 라우팅 구성.
- 편집 화면에 대주제/소주제 추가 버튼 및 모달을 추가해 타임라인 이벤트와 섹션을 즉시 생성할 수 있도록 했고, 목업 API(`createTimelineEvent`, `createSection`)와 상태 업데이트 로직을 연동했다.
- 대주제 선택에 따라 좌측 소주제 목록이 자동 필터링되도록 개선하고, 대주제 수정 모달/동작(`updateTimelineEvent`)을 추가했으며, 본문 저장 시 소주제 요약(Excerpt)이 즉시 갱신되도록 동기화 로직을 반영했다.
- 소주제 추가 시 입력 방식(텍스트/음성/파일)을 고르는 분기 모달을 도입하고, 텍스트 모달은 신규 컴포넌트(`AddSectionModal`)로 분리해 상태 관리·검증을 단순화했다. 음성/파일 흐름은 플레이스홀더 콘텐츠를 생성해 후속 녹음/처리 단계와 연동 준비까지 마무리.

### 편집 타임라인 확장 계획 (대주제/소주제 생성)

1. **UX 설계**
   - 타임라인 상단 우측에 “대주제 추가” 버튼을 배치하고, 모달/패널에서 제목·기간·메모 입력 후 생성되도록 한다.
   - 섹션 리스트 상단에는 “소주제 추가” 버튼을 추가해 현재 선택된 대주제에 연결된 섹션을 바로 만들 수 있게 한다.
   - 생성 시 사용자가 어느 대주제에 속하는지 명확히 인지할 수 있도록 현재 선택값을 표시하고, 필요 시 다른 대주제를 선택할 수 있는 선택 UI를 제공한다.
2. **데이터/Mock API**
   - `TimelineEvent`, `Section` 타입을 확장해 입력 필드를 수용하고, `lib/mock-api.ts`에 `createTimelineEvent`, `createSection` 등의 목업 함수를 추가한다.
3. **상태 업데이트 로직**
   - 생성 성공 시 `setTimeline`, `setSections`를 활용해 낙관적 업데이트를 적용하고, 새 항목을 자동으로 선택한다.
4. **후속 작업**
   - 생성 후 포커스 이동 및 기본 편집 흐름을 검토하고, CODEX에 결과를 기록한 뒤 lint/QA를 수행한다.

### 소주제 입력 UX 개선 세부 계획

1. **분기 모달 설계**  
   - “소주제 추가” 클릭 시 입력 방식 선택 모달을 띄워 `텍스트 작성 / 음성 녹음 / 파일 가져오기` 중 하나를 고르게 한다.  
   - 각 옵션별 설명과 예상 처리 시간을 명시, 취소/뒤로 가기 버튼도 제공.

2. **텍스트 작성 플로우**  
   - 기존 제목/요약/본문 입력 모달을 유지하되, 저장 후 에디터가 해당 섹션으로 스크롤·포커스되도록 한다.

3. **음성 녹음 플로우**  
   - 모달에서 음성 선택 시 우측 패널을 `녹음 → 전사 → 소단락 분할` 단계 UI로 전환.  
   - 녹음 완료 후 AI가 소단락(subsections) 리스트를 반환하면, 중앙 에디터에서 블록 형태로 노출·편집 가능하게 한다.  
   - 필요 시 소단락을 수동으로 병합/삭제할 수 있도록 UI(슬라이더/체크박스) 제공.

4. **파일 가져오기 플로우**  
   - 음성/텍스트 파일 업로드를 지원하고, 타입에 따라 전사 또는 요약 파이프라인을 수행.  
   - 결과물은 음성 플로우와 동일하게 소단락 블록으로 제공하고, 사용자가 빠르게 다듬을 수 있도록 한다.

5. **데이터 모델 확장**  
   - `Section`에 `subsections: Array<{ id; title; content; sourceType }>` 필드를 추가해 세부 블록을 관리.  
   - 메인 `content`는 subsections의 합본 또는 대표 요약으로 유지, excerpt는 첫 블록을 기반으로 생성.

6. **상태/로딩 관리**  
   - 분기 선택 후에는 Right Panel에서 진행 상태(녹음 중/전사 중 등)를 단계별 Progress UI로 표시.  
   - React Query/Zustand를 활용해 작업 진행 상태와 큐 처리 상태를 관리하고, 완료 시 중앙 에디터로 포커스 이동.

7. **QA 및 후속 작업**  
   - 각 입력 방식을 목업 API와 연결해 흐름을 검증하고, 실제 백엔드 연동 시 예상 API 스펙을 문서화.  
   - CODEX에 결과 기록 후 lint/테스트 실행.

## 향후 출판 플로우 고도화 제안 (CTO 공유용)

- **기술 스택**
  - 프런트엔드: Next.js(App Router) + React 유지, 상태/서버 통신 최적화를 위해 React Query 도입, 마법사 단계 상태는 Zustand·Recoil 등 경량 상태 관리로 보존, Tailwind로 UI 일관성 유지.
  - 백엔드: Next Route Handlers 또는 별도 BFF로 출판 옵션/견적/요청 API 제공, S3 등 객체 스토리지와 BullMQ·SQS 기반 큐로 PDF/EPUB/오디오 렌더링 처리, 외부 결제·알림(이메일/문자) API 통합.

- **UX 단계**
  1. Result 화면의 “편집 완료” 버튼 클릭 시 출판 마법사 진입.
  2. 1단계 `출판 방식 선택`: 실물책/전자책/오디오북 카드형 멀티 선택, 사이드 요약 패널로 선택 옵션 요약.
  3. 2단계 `세부 정보 설정`: 선택된 옵션별 폼(판형·커버·수량, EPUB/PDF·공유 범위, 보이스·러닝타임 등)을 동적으로 렌더링하고 실시간 비용 힌트 제공.
  4. 3단계 `견적 & 후속 안내`: 항목별 예상 비용/제작 일정 요약, 상담/제작 요청 CTA, 이야기 다시 보기·처음으로 돌아가기 옵션 노출.

- **아키텍처 구조**
  - UI 컴포넌트: 스텝퍼 컨테이너, 옵션 카드, 디테일 폼, 견적 리뷰 블록으로 모듈화.
  - 상태 관리: 마법사 컨텍스트/스토어에 단계 및 선택값 저장, React Query로 백엔드 견적/요청 API 호출, 로컬에서 계산 가능한 비용은 즉시 업데이트.
  - API 설계: `/api/publish/options`, `/api/publish/estimate`, `/api/publish/request` 등 엔드포인트 정의, 제작 진행 상태를 반환하거나 Webhook/폴링으로 알림 제공.
  - 비동기 처리: PDF/EPUB/오디오 렌더링 작업은 큐에 넣어 비동기 실행, 진행 상황은 알림·대시보드·폴링으로 노출.

- **확장 아이디어**
  - 주문 현황과 제작 진행, 파일 다운로드를 지원하는 사용자 대시보드.
  - 단계별 이탈률/옵션 선택 통계로 UX 최적화.
  - 접근성 강화(키보드 내비게이션, 대체 텍스트, 명확한 상태 피드백 등).
