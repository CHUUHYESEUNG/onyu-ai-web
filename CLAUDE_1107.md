Agent가 현재 UX 문제점을 상세히
  분석했습니다. 핵심은 30개 이상의 버튼, 협소한
  에디터 공간(30%), 복잡한 모달 워크플로우가
  난잡함의 주요 원인입니다.

  개선 방향을 제시하기 전에, 사용자님의 우선순위를
   파악하고 싶습니다:
⏺ User answered Claude's questions:
  ⎿  · edit 페이지에서 사용자가 가장 자주, 오래
     사용하는 핵심 작업은 무엇인가요? → 균형있게
     · 타임라인 이벤트와 섹션의 순서 조정(드래그,
     이동 버튼) 기능을 얼마나 자주 사용할 것
     같나요? → 자주 사용
     · 어떤 UX 스타일을 선호하시나요? →
     미니멀/집중
     · 좌측 섹션 목록과 우측 녹음 패널의 기본
     상태는 어떻게 하면 좋을까요? → 둘 다 펼친
     상태

⏺ 완벽합니다! 사용자님의 니즈를 정리하면:
  - ✅ 균형있는 작업: 글쓰기, 녹음, 구조 관리 모두
   중요
  - ✅ 자주 사용하는 순서 조정: 드래그/이동 기능이
   항상 접근 가능해야 함
  - ✅ 미니멀/집중 스타일: 핵심 작업에 집중, 부가
  기능은 숨김
  - ✅ 3열 레이아웃 유지: 둘 다 펼친 상태 기본

  이 조합에서 핵심은 **"기능은 유지하되 시각적
  복잡도를 줄이는 것"**입니다. 개선 플랜을
  제시하겠습니다.


Edit 페이지 UX 개선 플랜

     🎯 목표

     "기능 완전성 유지 + 시각적 복잡도 70% 감소 +
     작업 공간 50% 확대"

     ---
     Phase 1: 타임라인 영역 단순화 (30분)

     변경사항:

     1. 타임라인 헤더 제거
       - 별도 헤더 섹션 완전 제거
       - "대주제 추가" 버튼 → 타임라인 바 우측
     끝으로 이동
       - "대주제 수정" 버튼 → 타임라인 노드
     hover/선택 시 컨텍스트 메뉴로 이동
     2. 타임라인 노드 정리
       - 좌/우 이동 버튼 제거 (드래그만 사용)
       - hover 시에만 수정 아이콘 표시 (연필
     아이콘 1개)
       - 노드 크기 축소 (현재 대비 20% 작게)

     효과: 수직 공간 80px 확보, 버튼 6-10개 제거

     ---
     Phase 2: 섹션 목록 컴팩트화 (20분)

     변경사항:

     1. 이동 버튼 제거
       - "위로"/"아래로" 버튼 완전 제거
       - 드래그 핸들만 유지 (⋮⋮ 아이콘)
     2. 카드 디자인 간소화
       - 발췌문(excerpt)을 hover 시에만 표시
     (기본은 제목만)
       - 카드 높이 40% 감소
       - 검색창을 헤더 우측으로 이동 (돋보기
     아이콘 → 클릭 시 확장)
     3. 추가 버튼 개선
       - "소주제 추가" 버튼 → 상단 고정 + 아이콘만
      표시

     효과: 한 화면에 보이는 섹션 수 2배 증가, 버튼
      10-20개 제거

     ---
     Phase 3: 에디터 영역 집중화 (40분)

     변경사항:

     1. 에디터 내부 2열 그리드 제거
       - 소단락 관리를 우측 사이드바로 분리 (토글
     가능)
       - 기본 상태: 본문 작성만 전체 너비 사용
       - 소단락 보기: 우측 슬라이드인 패널 (300px)
     2. 에디터 헤더 최소화
       - 섹션 제목을 textarea 위에 인라인 배치
       - "버전 보기" 버튼 → 우측 상단 아이콘으로
     3. 저장 상태 개선
       - 하단 고정 바 제거
       - 우측 상단에 실시간 저장 인디케이터 추가
       - "자동 저장됨 · 3초 전" 텍스트 표시

     효과: 에디터 실질 너비 50% → 80% 증가

     ---
     Phase 4: 녹음 패널 정리 (25분)

     변경사항:

     1. 처리 단계 타임라인 접기
       - 기본 상태: 접힌 상태 (한 줄 요약만 표시)
       - "자세히 보기" 클릭 시 확장
       - 녹음 중/처리 중에만 자동 확장
     2. 녹음 컨트롤 고정
       - 녹음 버튼 영역을 상단에 고정 (스크롤
     무관)
       - 파일 업로드를 녹음 버튼 하단으로 이동
     3. 히스토리 카드 간소화
       - 기본 상태: 전사 텍스트 3줄까지만 표시
     (더보기로 확장)
       - 수정/삭제/삽입 버튼 → hover 시에만 표시

     효과: 패널 스크롤 50% 감소, 녹음 버튼 항상
     접근 가능

     ---
     Phase 5: 모달 워크플로우 개선 (30분)

     변경사항:

     1. 소주제 추가 단순화
       - 2단계 모달 → 1단계로 통합
       - "텍스트/음성/파일" 선택을 모달 내부
     탭으로 이동
       - 또는 기본은 텍스트 입력, 우측에 "음성으로
      추가" 버튼
     2. 대주제 수정 인라인화
       - 모달 대신 타임라인 노드 클릭 시 인라인
     편집 (Notion 스타일)
       - 입력 필드 3개를 노드 아래 드롭다운으로
     표시

     효과: 클릭 수 30% 감소, 작업 플로우 단절
     최소화

     ---
     Phase 6: 시각적 정리 (15분)

     변경사항:

     1. 배경색 통일
       - 모든 패널을 bg-navy-900로 통일
       - 카드/섹션은 bg-navy-800/50으로 미세하게
     구분
       - border는 필수 구분선만 남기고 제거
     2. 여백 일관성
       - 모든 패널의 패딩을 p-4로 통일
       - 카드 간격을 gap-3로 통일
     3. 토글 버튼 개선
       - absolute 위치 → 패널 내부 헤더로 이동
       - 아이콘 크기 통일

     효과: 통합된 워크스페이스 느낌, 시각적 노이즈
      40% 감소

     ---
     Phase 7: 반응형 최적화 (20분)

     변경사항:

     1. 패널 너비 조정
       - 좌측 섹션 목록: 25% → 20% (min-w-240px)
       - 우측 녹음 패널: 25% → 22% (min-w-260px)
       - 중앙 에디터: 50% → 58% (유동)
     2. 1400px 이하 대응
       - 기본 상태를 "좌측만 펼침" 또는 "우측만
     펼침"으로 전환
       - localStorage에 마지막 상태 저장

     효과: 에디터 작업 공간 16% 추가 확보

     ---
     📊 예상 개선 효과

     | 지표        | 개선 전         | 개선 후   |
      변화    |
     |-----------|--------------|--------|-------|
     | 화면 내 버튼 수 | 30-40개       | 10-15개 |
      -65%  |
     | 에디터 실질 너비 | 30%          | 50-58% |
     +80%  |
     | 모달 단계     | 2-3단계        | 1단계    |
      -60%  |
     | 수직 스크롤    | 타임라인 헤더 80px | 제거
         | +80px |
     | 인지 부하     | ★★★★★        | ★★☆☆☆  |
     -60%  |

     ---
     🔍 주요 설계 원칙

     1. Progressive Disclosure: 자주 사용하지 않는
      기능은 hover/클릭 시 노출
     2. 드래그 우선: 버튼보다 드래그가 더
     직관적이고 공간 효율적
     3. 인라인 편집: 모달을 최소화하고 컨텍스트
     내에서 편집
     4. Sticky Controls: 핵심 컨트롤(녹음 버튼)은
     항상 접근 가능하게
     5. Consistent Spacing: 일관된 여백과
     배경색으로 통합감 제공

     ---
     ⚠️ 주의사항

     - 모든 기능 유지 (제거 없음, 재배치만)
     - 드래그 기능이 핵심이므로 드래그 UX 개선
     필요
     - localStorage로 사용자 선호 패널 상태 저장

     ---

## 2025-11-07 Codex 진행 현황

- Phase 1 착수: `components/edit/timeline-bar.tsx`를 최소 형태로 정리하고 노드 크기를 20% 축소, 인라인 편집 진입 아이콘을 hover 상태에서만 노출하도록 구성. `onEditEvent` 훅을 추가해 `/projects/[projectId]/edit`에서 바로 대주제 수정 모달을 열 수 있게 했습니다.
- Phase 2 적용: `components/edit/section-list.tsx`에 고정 헤더·토글형 검색·아이콘 전용 “추가” 버튼을 통합하고, 카드 높이·여백을 줄여 한 화면에 더 많은 소주제를 노출. 발췌문은 hover/선택 시에만 펼쳐지도록 조정했습니다.
- 레이아웃 정비: 좌측 패널 상단의 중복 헤더를 제거하고 `SectionList`에 `onAddSection`을 전달하는 방식으로 컴팩트 UI를 유지했습니다.
- 검증: `npm run lint` 실행 시 기존에 존재하던 `@ts-nocheck`, `any` 타입, 미사용 변수 관련 오류가 다시 보고됩니다(예: `app/api/transcribe/route.ts`, `components/edit/recording-panel.tsx`). 이번 변경과 직접적인 연관은 없으며, 추후 타입 정리 작업이 필요합니다.
